# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (never commit secrets)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Mobile app build output (regenerated by npm run build:student / build:attend)
dist-student
dist-attend

# Capacitor native build artifacts and machine-specific config
android/local.properties
android-attend/local.properties
*.keystore
*.jks

# Firebase client config — safe to commit if you want CI builds, but it is
# tied to one Firebase project, so keep it deliberate rather than accidental.
# android/app/google-services.json
